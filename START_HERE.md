# Start Here

## Windows 启动

双击：

```text
start_windows.bat
```

脚本会启动本地服务，并自动打开浏览器。

## 当前版本

当前是 v0.2 工作台空壳：

- 有浏览器工作台。
- 有 capability registry。
- 默认能力为 `SPA/SHA KTS`。
- 可以执行 dry-run，并将运行记录保存到 `runs/`。

还未实现：

- DOCX 解析。
- KTS 生成。
- DOCX 导出。

