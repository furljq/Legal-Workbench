# Start Here

## Windows 启动

双击：

```text
start_windows.bat
```

脚本会启动本地服务，并自动打开浏览器。

## 当前版本

当前是 v0.3 文件解析版：

- 有浏览器工作台。
- 有 capability registry。
- 默认能力为 `SPA/SHA KTS`。
- 可以测试工作台连通状态。
- 可以上传多个 Word 文件。
- 可以读取全部正文段落和表格行，并粗略识别文件类型。

还未实现：

- KTS 生成。
- DOCX 导出。
